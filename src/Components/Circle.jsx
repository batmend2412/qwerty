export const Circle = ({ className }) => {
  return (
    <div
      className={`opacity-25 w-[140px] h-[140px] absolute border-solid border-2 border-gray-500 rounded-full -translate-1/2 ${className}`}
    ></div>
  );
};
